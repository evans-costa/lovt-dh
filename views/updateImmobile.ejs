<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Alterar imóveis</title>
</head>
<body>
  <form method="post" action="?_method=PUT" enctype="multipart/form-data">
    <label for="picture">Foto</label>
    <input id="picture" name="picture" type="text" value="<%=immobile.picture%>"/>

    <label for="file">Foto</label>
    <input id="file" name="file" type="file" accept="image/png, image/jpeg"/>

    <label for="price">Preço</label>
    <input id="price" name="price" type="text" value="<%=immobile.price%>"/>

    <label for="status">Status</label>
    <input id="status" name="status" type="text" value="<%=immobile.istatus%>"/>

    <label for="description">Descrição</label>
    <input id="description" name="description" type="text" value="<%=immobile.idescription%>"/>

    <label for="category">Categoria</label>
      <select name="category">
        <% for (let category of categories) { %>
          <option value="<%=category.id%>">
            <%=category.Cname%>
          </option>
        <% } %>
      </select>

    <button type="submit">Alterar</button>
  </form>

  <form method="post" action="?_method=DELETE">
    <button type="submit">Excluir</button>
  </form>
</body>
</html>